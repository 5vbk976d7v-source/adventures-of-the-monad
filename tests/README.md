# Browser navigation check

Start the app with `npm start`, open it in Chrome, and run the contents of `navigation.browser.js` in DevTools. Then run `await testAtlasNavigation()`. The script exercises the real UI and reports failed navigation checks in the console. Reload the page before running it again.
